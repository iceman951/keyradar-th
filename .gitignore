node_modules
.svelte-kit
build
coverage
playwright-report
test-results
.DS_Store
.dev.vars
.wrangler
scripts/.generated
# Regenerated by `pnpm wrangler types` (CI does this before type-checking);
# it is derived from wrangler.toml + the installed workerd, not source.
worker-configuration.d.ts
